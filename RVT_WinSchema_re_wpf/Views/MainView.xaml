<Window x:Class="RVT_WinSchema_re_wpf.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Схема окон"
        Height="260"
        Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выберите вид для схемы:"
                   FontWeight="Bold"
                   Margin="0,0,0,5"/>

        <ComboBox Grid.Row="1"
                  Height="25"
                  Margin="0,0,0,15"

                  ItemsSource="{Binding Views}"
                  SelectedItem="{Binding SelectedView}"

                  DisplayMemberPath="Name"/>

        <CheckBox Grid.Row="2"
                  Content="Вычислить площадь"
                  IsChecked="{Binding CalculateArea}"
                  Margin="0,0,0,5"/>

        <CheckBox Grid.Row="3"
                  Content="Учитывать монтажные зазоры"
                  IsChecked="{Binding IncludeGaps}"
                  Margin="0,0,0,15"/>

        <Button Grid.Row="4"
                Content="Сформировать схему"
                Height="30"
                Width="180"
                HorizontalAlignment="Center"
                Command="{Binding GenerateSchemaCommand}"/>

    </Grid>

</Window>
